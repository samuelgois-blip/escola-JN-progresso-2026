function soma(a, b) {
    return a + b;
  }
  
  // Exemplo de uso
  let resultado = soma(5, 3);
  
  console.log(resultado); // 8
  
module.exports = soma;

resultado = soma(5,10)
const soma = require ()